diff --git a/dist/auth/types.d.ts b/dist/auth/types.d.ts
index 80eeb8383fd5e55d4fe9194d7d85e965ce0771fc..64b6ffced4fc67a25d88299a7a4ff62e26e9b5ac 100644
--- a/dist/auth/types.d.ts
+++ b/dist/auth/types.d.ts
@@ -125,6 +125,14 @@ export interface AuthLoginCallbacks {
     signal?: AbortSignal;
     prompt(prompt: AuthPrompt): Promise<string>;
     notify(event: AuthEvent): void;
+    /**
+     * Optionally customize the successful local OAuth callback page.
+     * Only semantic, non-sensitive page state is supplied.
+     */
+    renderOAuthCallbackPage?(page: {
+        readonly provider: "openai-codex";
+        readonly status: "success";
+    }): string;
 }
 /**
  * Api-key auth: stored key/provider env plus ambient sources (env vars, AWS
diff --git a/dist/utils/oauth/openai-codex.d.ts b/dist/utils/oauth/openai-codex.d.ts
index 2edcbefa03c344b44cd7faa516f47502c68d8d59..363ae8a6d0ad47d5cafb23440cdaf748387b4040 100644
--- a/dist/utils/oauth/openai-codex.d.ts
+++ b/dist/utils/oauth/openai-codex.d.ts
@@ -34,6 +34,10 @@ export declare function loginOpenAICodex(options: {
     onPrompt: (prompt: OAuthPrompt) => Promise<string>;
     onProgress?: (message: string) => void;
     onManualCodeInput?: () => Promise<string>;
+    renderOAuthCallbackPage?: (page: {
+        readonly provider: "openai-codex";
+        readonly status: "success";
+    }) => string;
     originator?: string;
 }): Promise<OAuthCredentials>;
 /**
diff --git a/dist/utils/oauth/openai-codex.js b/dist/utils/oauth/openai-codex.js
index 3b5925156d44b0c12409e83f7cb9e5e488cfc754..e65c1b7561aec55022344da3a11137f70020e5ea 100644
--- a/dist/utils/oauth/openai-codex.js
+++ b/dist/utils/oauth/openai-codex.js
@@ -241,7 +241,26 @@ async function createAuthorizationFlow(originator = "pi") {
     url.searchParams.set("originator", originator);
     return { verifier, state, url: url.toString() };
 }
-function startLocalOAuthServer(state) {
+function renderSuccessfulCallbackPage(renderer) {
+    const fallback = () => oauthSuccessHtml("OpenAI authentication completed. You can close this window.");
+    if (!renderer) {
+        return fallback();
+    }
+    try {
+        const rendered = renderer(Object.freeze({ provider: "openai-codex", status: "success" }));
+        return typeof rendered === "string" && rendered.trim().length > 0 ? rendered : fallback();
+    }
+    catch {
+        return fallback();
+    }
+}
+function setCallbackResponseHeaders(res) {
+    res.setHeader("Cache-Control", "no-store");
+    res.setHeader("Content-Security-Policy", "default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'");
+    res.setHeader("Referrer-Policy", "no-referrer");
+    res.setHeader("X-Content-Type-Options", "nosniff");
+}
+function startLocalOAuthServer(state, renderOAuthCallbackPage) {
     if (!_http) {
         throw new Error("OpenAI Codex OAuth is only available in Node.js environments");
     }
@@ -257,6 +276,7 @@ function startLocalOAuthServer(state) {
     });
     const server = _http.createServer((req, res) => {
         try {
+            setCallbackResponseHeaders(res);
             const url = new URL(req.url || "", "http://localhost");
             if (url.pathname !== "/auth/callback") {
                 res.statusCode = 404;
@@ -279,7 +299,7 @@ function startLocalOAuthServer(state) {
             }
             res.statusCode = 200;
             res.setHeader("Content-Type", "text/html; charset=utf-8");
-            res.end(oauthSuccessHtml("OpenAI authentication completed. You can close this window."));
+            res.end(renderSuccessfulCallbackPage(renderOAuthCallbackPage));
             settleWait?.({ code });
         }
         catch {
@@ -364,7 +384,7 @@ export async function loginOpenAICodexDeviceCode(options) {
  */
 export async function loginOpenAICodex(options) {
     const { verifier, state, url } = await createAuthorizationFlow(options.originator);
-    const server = await startLocalOAuthServer(state);
+    const server = await startLocalOAuthServer(state, options.renderOAuthCallbackPage);
     options.onAuth({ url, instructions: "A browser window should open. Complete login to finish." });
     let code;
     try {
@@ -482,6 +502,7 @@ export const openaiCodexOAuth = {
                     placeholder: REDIRECT_URI,
                     signal: manualAbort.signal,
                 }),
+                renderOAuthCallbackPage: callbacks.renderOAuthCallbackPage,
             });
             return { ...credentials, type: "oauth" };
         }
